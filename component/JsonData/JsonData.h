#ifndef __JSONDATA_H__
#define __JSONDATA_H__

#include <esp_log.h>
#include <esp_err.h>
#include <string.h>

static const char *TAG = "Json_Data";

#if (0)
typedef struct {
    char *name;
    int32_t valueData;
} object_st;

typedef struct jsonData
{
    object_st *object;
    object_st *objectNext;
} jsonData;

esp_err_t create_new_object(const char *nameObject, const )
#endif



#endif